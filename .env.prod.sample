# Где лежат образы:
#   ghcr.io/<org_or_user>
#   docker.io/<user>
REGISTRY=ghcr.io/your-org

# Имя репозитория (совпадает с docker-bake.hcl)
REPO=tg-bots

# Тег образов: версия/sha/дата — выбираешь сам (в Makefile по умолчанию git-SHA)
VERSION=0.2.0

# Опционально: общий TZ и т.п.
TZ=Europe/Amsterdam
